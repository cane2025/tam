import { Staff } from '../types';
import { AppState } from '../types';
interface StaffSummaryProps {
    staff: Staff;
    state: AppState;
}
export default function StaffSummary({ staff, state }: StaffSummaryProps): import("react/jsx-runtime").JSX.Element;
export {};
